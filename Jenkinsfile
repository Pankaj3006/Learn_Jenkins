pipeline {
  agent any
  stages {
    stage('Pre-build') {
      steps {
        echo 'pre-build'
      }
    }

    stage('Build') {
      steps {
        echo 'Build'
      }
    }

    stage('Post-build') {
      steps {
        echo 'post-build'
      }
    }

  }
}